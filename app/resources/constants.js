import colors from 'resources/colors';
import images from 'resources/images';

const constants = {
  serviceDefaultUrl: 'http://apiurl.com/',

  /* UI CONSTANTS */

  menuIcon: images.iconMenu,
  backIcon: images.iconBack,
};

module.exports = constants;
